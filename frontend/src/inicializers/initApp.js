const initAppState =  {
		// user data from memory,
		// 		validate cookie with server
		// get coockie from memory
		// check cookie 
		isLoading: null,
		isError: null,
}

export default initAppState;
