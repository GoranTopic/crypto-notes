const initUser = {
		// user data from memory,
		// 		validate cookie with server
		// get coockie from memory
		// check cookie 
		username: null,
		firstname: null,
		latname: null,
		isSignedIn: false,
		cookie: null,
}


export default initUser;
